import styles from './enable_plugins.module.scss';

const EnablePluginsComponent: React.FC = () => {
	return (
        <div className={styles.enable_plugins}>
            enable plugins
        </div>
	)
};

export default EnablePluginsComponent;
