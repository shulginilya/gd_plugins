import styles from './plugins_container.module.scss';

const PluginsContainer: React.FC = () => {
	return (
		<>
			<section className={styles.plugins_container}>
			</section>
		</>
	)
};

export default PluginsContainer;
